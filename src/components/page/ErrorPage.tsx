/* Error Page */
import { FC } from "react"
import { ErrorPageProps } from "../../interface/interface"

export const ErrorPage = () => {
    return <> <h2 className="card-panel-vertical">Unknown error, check application</h2>
    </>
}